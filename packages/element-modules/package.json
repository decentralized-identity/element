{
  "name": "@transmute/element-modules",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1",
    "@transmute/element-core": "0.0.0",
    "bip39": "^3.0.2"
  },
  "scripts": {
    "build": "react-scripts build",
    "test": "npm run test:node && npm run test:react",
    "test:react": "CI=true react-scripts test --env=jsdom --testPathIgnorePatterns=./src/Node",
    "test:node": "CI=true react-scripts test --env=node --testPathIgnorePatterns=./src/React",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
